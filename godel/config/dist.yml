products:
  bouncer:
    build:
      main-pkg: ./main
      os-archs:
        - os: darwin
          arch: amd64
        - os: linux
          arch: amd64

      version-var: github.com/palantir/bouncer/cmd.version
group-id: com.palantir.arch
